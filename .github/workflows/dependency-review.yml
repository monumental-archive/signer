# Dependency review on pull requests, via the shared org workflow.
name: dependency-review

on:
  pull_request:

permissions: {}

jobs:
  review:
    uses: monumental-archive/.github/.github/workflows/dependency-review.yml@dddc13f2bfc4348998dcd7ee84c6f37faa4bd4cd # v1.23.0
    permissions:
      contents: read # checkout for the diff of manifests
